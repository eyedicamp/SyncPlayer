{
  "name": "syncplayer",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -k \"npm:dev:server\" \"npm:dev:web\"",
    "dev:web": "npm run dev --workspace @syncplayer/web",
    "dev:server": "npm run dev --workspace @syncplayer/server",
    "build": "npm run build --workspace @syncplayer/server && npm run build --workspace @syncplayer/web",
    "typecheck": "npm run typecheck --workspace @syncplayer/server && npm run typecheck --workspace @syncplayer/web",
    "lint": "npm run lint --workspace @syncplayer/web"
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.9.0"
  }
}
